/* games/ocean.js — game package (being built; brief in GAMES-FIRST.md roadmap).
   Stub registers nothing so the tag is safe to ship ahead of the package. */
(function () { "use strict"; if (!window.CBZ || !CBZ.games) return; })();
