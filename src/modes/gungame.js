/* ============================================================
   modes/gungame.js — GUN GAME: weapon-ladder deathmatch on a map you choose
   (the prison, the disaster island, …). STUB — the gungame builder wave
   replaces this file wholesale; the script tag in index.html already loads it.
============================================================ */
(function () {
  "use strict";
  // Intentionally inert until the mode ships. The title button exists; picking
  // it before this file is real just falls back to escape (state.js default).
})();
