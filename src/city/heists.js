/* ============================================================
   city/heists.js — planned ROBBERIES / big scores (the marquee get-rich loop).

   Corner-store stickups, jewelry smash-and-grabs, armored trucks, and bank
   jobs: case it, hit it, survive the heat, escape with the bag. Payout scales
   with risk + your crew + the wanted heat it draws. This is the high-end faucet
   that funds the kingpin climb.

   (Filled by the heists build agent. Stub keeps boot order + a no-op API stable.)
   ============================================================ */
(function () {
  const CBZ = window.CBZ;
  if (!CBZ) return;
})();
