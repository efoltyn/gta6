/* ============================================================
   city/aircraft.js — police AIR support: a pursuit helicopter and, at the top
   wanted tier, missile-firing attack runs + fighter-jet strafes.

   (Filled in by the aircraft build agent. Stub keeps the script tag + boot
   order stable so the rest of the city loads regardless.)
   ============================================================ */
(function () {
  const CBZ = window.CBZ;
  if (!CBZ) return;
})();
